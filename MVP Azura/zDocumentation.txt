CSS → visual only (opacity/transform)
JS → structural (display on/off)




🟦 ui.js    - “What user sees”

🟨 state.js - “What user has typed”

🟥 logic.js - “Is it correct or wrong?”



🟦 STATE (truth)
state.userInput
state.current

🟨 UI
renders overlay (hint + typed)
NEVER stores real input

🟥 LOGIC
reads ONLY state
never reads DOM